# ⚖️ Призма достоверности — головной документ проекта

**Статус: этот файл — главный (родительский) документ проекта.** `Readme.md` и весь код остаются
как есть, но с этого момента любой технический вывод (свой или предложенный извне, в т.ч. любым
ИИ) обязан пройти через два правила ниже, прежде чем считаться основанием для действия. Файл не
меняет код и не предполагает немедленных правок — это документ анализа и фильтрации версий.

## Как появилась эта Призма

Пользователь загуглил симптом ("белый экран после перехода с Arduino на STM32") и получил от
стороннего ИИ-чата развёрнутый ответ с гипотезами. Один из "аргументов" был прислан в виде
**скриншота этого самого ИИ-чата** (файл `.claude/sessions/photo_2026-09-19_19-27-57.jpg`).

**Важная поправка правила №1, применённая к нему самому:** это не фотография платы и не схема —
это текст, который сгенерировал другой ИИ. Сам по себе такой скриншот **не является доказательством**
ничего про реальное железо — ровно так же, как и любой другой текст ИИ, включая мои собственные
предыдущие ответы в этом проекте, которые пользователь справедливо назвал не сработавшими.
Поэтому главный закон Призмы ниже принят **не потому, что так написано на скриншоте**, а потому что
он независимо подтверждён при прямой проверке первичных источников (см. таблицу доказательств).

## Правило №1 Призмы — проверка независимыми источниками

Любое техническое утверждение (от любого ИИ, включая это сообщение) допускается в документацию
проекта только с одной из трёх пометок:

| Пометка | Что значит |
|---|---|
| ✅ ПОДТВЕРЖДЕНО | Есть прямая цитата из первичного источника (форум с указанием автора, официальный даташит/схема, реально работающий чужой код), и по возможности — независимое второе подтверждение |
| ⚠️ ЧАСТИЧНО / ПРАВДОПОДОБНО | Механизм подтверждён в общем виде, но конкретное число/порог/шаг для НАШЕГО железа не проверен — нужен эксперимент |
| ❌ НЕ ПОДТВЕРЖДЕНО | Правдоподобно звучит, но при проверке первичных источников подтверждения не нашлось, либо найдено прямое противоречие |

## Правило №2 Призмы (главный закон) — это НЕ настоящий SPI-дисплей

> Плата Waveshare 3.5" TFT Touch Shield (ILI9486, 480×320) физически — это дешёвая **параллельная
> 8080-16 матрица**. Сигналы, которые мы называем "SPI" (SCLK/MOSI/CS/DC), на самом деле поступают
> на сдвиговые регистры на обратной стороне платы, которые аппаратно превращают последовательный
> поток в параллельную шину, которая уже дёргает ножки самой матрицы ILI9486.

**Статус: ✅ ПОДТВЕРЖДЕНО** — независимо, двумя разными именованными экспертами на двух разных
форумах, без ссылки друг на друга:

| # | Источник (первичный, прочитан напрямую) | Кто | Цитата |
|---|---|---|---|
| 1 | [forum.arduino.cc, тема "white screen ... Waveshare 4 ILI9486 TFT_eSPI ⟨SOLVED⟩", пост №2](https://forum.arduino.cc/t/only-white-screen-with-stm32-nucleo-64-l467rg-waveshare-4-ili9486-tft-espi-solved/899646/2) | **david_prentice** (соавтор/эксперт экосистемы TFT_eSPI, отвечает на десятках тем про ILI9486) | *"The Waveshare abortion consists of using Shift Registers to receive SPI commands. Then bit-banging the result to a cheap Parallel 8080-16 ILI9486 Panel."* |
| 2 | [forum.pjrc.com, тема ILI9488_t3, пост №619](https://forum.pjrc.com/index.php?threads/ili9488_t3-support-for-the-ili9488-on-t3-x-and-beyond.55735/page-25) | **ImpulseAdventure** (автор Arduino-библиотеки `Waveshare_ILI9486`, отдельного независимого проекта) | *"...the pair of 74HC4094 shift registers to emulate a SPI interface on the 8080-16 wired ILI9486"* |

**Обновление (визуальный осмотр платы пользователем, 2026-09-20): ✅ ПОДТВЕРЖДЕНО на 100%.**
Это уже не литературный источник, а данные первого порядка — прямой осмотр собственной платы
пользователем сильнее любой цитаты с форума. Маркировка микросхем на обратной стороне нашего
конкретного шилда: **1× `74HC04D`, 1× `74HC4040D`, 2× `74HC4094D`** — совпадает один в один с тем,
что до этого было только предположением из агрегированной выдачи поисковика (⚠️ частично). Состав
логики шилда теперь подтверждён напрямую, а не по вторичному источнику: `74HC04` — буфер/инвертор,
`74HC4040` — двоичный счётчик (формирует последовательность защёлкивания/строба для двух `74HC4094`),
`74HC4094` × 2 — сами 8-битные сдвиговые регистры-защёлки (16 бит на двоих, под 8080-16 шину панели).

### Карта архитектуры (реверс-инжиниринг сигнального пути)

```
STM32 (SPI2 hw peripheral)                Обратная сторона шилда              Панель ILI9486
──────────────────────────                ──────────────────────              ───────────────
PB13 SCLK ───────────────────────────────▶│                     │
PB15 MOSI ───────────────────────────────▶│  74HC4094 × 2        │──▶ 16 линий параллельной шины
PC0  LCD_CS ──────────────────────────────▶│  (сдвиг + защёлка)   │      (D0..D15, 8080-16 интерфейс)
PC1  LCD_DC ───────────┐                  │  + 74HC04 (буфер)    │
                        └─────────────────▶│  + 74HC4040 (счётчик)│──▶ CS/RS/WR панели формируются
PC2  LCD_RST ──────────────────────────────▶ (сброс идёт напрямую, в обход регистров) ──▶ RESET панели
PC3  LCD_BL ───────────────────────────────▶ (подсветка, напрямую, в обход регистров) ──▶ LED панели

PB14 MISO ◀────────────────────────────────  НЕ подключено к ILI9486.
                                              Подключено только к XPT2046 (тач), через TP_CS.
```

**Ключевое следствие для чтения:** обратного пути "панель → STM32" в этой архитектуре нет вообще —
сдвиговые регистры работают только на запись (serial-in → parallel-out), физически не умеют отдавать
данные назад. Единственная реальная линия чтения на этой плате — MISO тач-контроллера XPT2046,
и она никак не связана с ILI9486.

## Следствия закона — каждое со своим статусом достоверности

| Следствие | Статус | Обоснование |
|---|---|---|
| Чтение ID-регистра ILI9486 (`0xD3`) в принципе невозможно на этой плате — это не баг прошивки | ✅ ПОДТВЕРЖДЕНО | Прямое следствие закона (сдвиговый регистр = только запись) + собственные данные проекта: `LCD_ReadID` дважды (на /32 и на /256) дал одинаковый `FF FF FF` — ровно то, что даёт "повисшая" в лог.1 линия MISO, а не искажение от скорости |
| Инициализация ILI9486 должна идти "вслепую", без проверки ID | ✅ ПОДТВЕРЖДЕНО | Прямое следствие предыдущего пункта. Дополнительно подтверждено чтением кода `RobertoBenjami/stm32_graphics_display_drivers`: `ili9486_ReadID()` там существует, но **не вызывается** внутри `ili9486_Init()` — инициализация всегда "вслепую" |
| SPI-скорость нужно занижать до 2–4 МГц, иначе сдвиговые регистры "не успевают" | ❌ НЕ ПОДТВЕРЖДЕНО для этого проекта (опровергнуто собственным же референсом) | Официальная и уже признанная рабочей прошивка Waveshare для STM32 в этом же пакете (`STM32/XNUCLEO-F103RB/lcd3in5-demo/Src/spi.c`) гоняет **этот же шилд** на APB2=72 МГц (HSE 8 МГц ×9), `BaudRatePrescaler=4` → **SPI = 18 МГц**. Это втрое быстрее наших текущих 6.25 МГц — и работает. Значит текущая скорость проекта уже внутри доказанно рабочего диапазона; занижать её как "первое, что нужно попробовать" — не обосновано |
| Нужен SPI Mode 3 (CPOL=1/CPHA=1) вместо Mode 0 | ❌ НЕ ПОДТВЕРЖДЕНО для этого проекта (опровергнуто тем же референсом) | Тот же официальный `spi.c`: `CLKPolarity=LOW`, `CLKPhase=1EDGE` — это Mode 0, ровно как сейчас в проекте. Довод "у RobertoBenjami общий SPI-слой настроен на Mode 3" (это правда, см. ниже) не переносится на конкретно эту плату — это разные проекты с разной SPI-обвязкой |
| Нужны `__NOP()` между переключением CS/DC и вызовом SPI из-за "гонки сигналов" на STM32H7 | ❌ НЕ ПОДТВЕРЖДЕНО | В `RobertoBenjami/.../lcd_io_spi.c` (проверено для 8 разных семейств STM32, включая быстрые F7) GPIO CS/DC переключается и сразу вызывается аппаратный SPI-transmit, никаких NOP/задержек между ними нет — и это рабочий, используемый код. Гипотеза похожа на общий "правдоподобный ИИ-текст" без опоры на реальный код |
| Проблемы FIFO/DataSize в SPIv3 на H7 | ❌ НЕ ПОДТВЕРЖДЕНО, уже проверялось раньше | В проекте уже `DataSize=8Bit`; гипотеза про ручное ожидание `SPI_FLAG_EOT` уже опровергалась в сессии 2026-09-13 чтением исходников HAL (`HAL_SPI_Transmit` сам ждёт EOT). Ничего нового не найдено |
| `RobertoBenjami/stm32_graphics_display_drivers` — "готовая, отлаженная библиотека под этот экран" | ⚠️ ЧАСТИЧНО, преувеличено в пересланном тексте | Репозиторий реальный и активно используемый, содержит `Drivers/lcd/ili9486/ili9486.c` (протокол команд ILI9486 общий, полезен для сверки) и generic `lcd_io_spi.c` для семейств **f0/f1/f2/f3/f4/f7/l0/l1/l4** — но папки **`stm32h7xx` в репозитории нет**. Готового порта под наш H723 там просто не существует, его пришлось бы делать самим по образцу F7 |
| Форумная тема (Nucleo-L467RG) — тот же симптом "белый экран" | ✅ ПОДТВЕРЖДЕНО, но с другой причиной | Реальная тема, реально решена. Но конкретная причина у автора темы — использование неправильного/самодельного файла настроек `TFT_eSPI` вместо готового `User_Setups/Setup33_RPi_ILI9486_STM32.h`. У нас `TFT_eSPI` не используется вообще (чистый HAL), поэтому сам фикс не переносится напрямую — переносится только диагностика архитектуры (закон о сдвиговых регистрах), которую в этой теме и озвучил david_prentice |

## Переосмысление наших же диагностических данных через призму (без изменения кода)

- **Осциллограф ("все 6 линий в норме", сессия 2026-09-13).** Вывод не отменяется — уровни и
  переходы на пинах STM32 подтверждённо есть. Но с учётом закона нужно уточнение: щуп проверял
  сигнал **на выходе STM32**, а не на входе сдвиговых регистров и не после них (на параллельной шине
  к самой матрице). "Чистый" сигнал на STM32-стороне не гарантирует, что сдвиговый регистр
  корректно защёлкивает и ретранслирует его дальше — это разные точки цепи, и вторая никогда не
  проверялась (щупом туда физически не подлезть без выпайки шилда).
- **Двукратное чтение ID = `FF FF FF` (на /32 и на /256).** Раньше это было "непонятным результатом,
  требующим дальнейшей проверки". Через призму — это **ожидаемый, объяснённый результат**, а не
  загадка: закон предсказывает именно "плавающую" линию MISO при попытке читать из ILI9486, что и
  видно. Вопрос закрыт, дальше это перепроверять не нужно.
- **Гипотеза "может, скорость чтения слишком высокая для ILI9486" (отклонена в 2026-09-13).**
  Через призму подтверждается тем же выводом с другой стороны: раз линия физически не подключена к
  панели, скорость вообще не могла повлиять на этот конкретный тест — отклонение гипотезы было
  правильным, но по не до конца понятой на тот момент причине.
- **Новый факт, ранее не учтённый: сравнение скорости с XNUCLEO-F103RB (18 МГц, Mode 0, тот же
  шилд, официально рабочий).** Это меняет приоритеты: скорость и режим SPI как причина белого
  экрана в `LCD_FillScreen` теперь выглядят маловероятными (см. таблицу выше) — раньше на этом
  направлении просто не было данных для сравнения.
- **Что закон НЕ объясняет.** Сам факт, почему `LCD_FillScreen()` (запись, не чтение) даёт ровный
  белый экран при побайтово сверенном с двумя рабочими источниками протоколе — **остаётся открытым
  вопросом**. Закон о сдвиговых регистрах закрывает загадку с чтением ID, но не создаёт готового
  объяснения белого экрана при записи. Ниже — одна новая, явно помеченная как непроверенная,
  гипотеза, вытекающая из самого закона.

### Новая гипотеза, которую даёт закон (⚠️ не проверена, требует эксперимента, не путать с подтверждённым фактом)

Если между переключением `LCD_CS`/`LCD_DC` и самой SPI-транзакцией сдвиговый регистр ожидает не
просто "чистый" уровень, а конкретное количество тактов/строб-импульсов для защёлкивания (регистр
"store", а не просто "shift") — а наш код (как и оба референса) дёргает CS вокруг каждой отдельной
посылки байта — расхождение может быть не в скорости и не в CPOL/CPHA, а в том, **сколько именно
байт/тактов подряд идёт между поднятиями CS** при передаче многобайтных параметров и, особенно, при
потоковой заливке `RAMWR` (0x2C), где CS не отпускается сотни байт подряд — а на референсах заливка
может идти по-другому (например, более мелкими пакетами). Это не подтверждённый факт, а направление
для следующего эксперимента (сравнить именно структуру транзакций при заливке, а не только байты
команд).

## Сессия 2026-09-20: результат теста тача — не однозначен, делать выводы рано

Пользователь выполнил тест из README/предложения №1 (Live Expressions, `tp_x`/`tp_y`), нажимал
сильно (учитывая, что на Arduino этому же резистивному тачу тоже нужно сильное нажатие). **Результат:
оба значения оставались нулевыми, изменений не было ни разу.**

**Важно (Правило №1 применительно к своим же выводам): нельзя сразу записывать это как "приём по
MISO/SPI сломан на уровне железа".** Это только один из нескольких равноправных на данный момент
вариантов — и у как минимум одного из них уже есть прецедент в этом же проекте:

| Гипотеза | Статус | Почему рассматривается |
|---|---|---|
| Та же методическая ошибка, что и в сессии 2026-09-19 (вкладка **Expressions** вместо **Live Expressions**, либо программа стояла на автостопе CubeIDE в начале `main()` и не была реально в состоянии **Running**, либо колонка Value была не видна/обрезана) | ⚠️ наиболее вероятная, не исключена | Ровно эти три ошибки уже были описаны и задокументированы в README как реально случившиеся в предыдущей попытке этого же теста — статистически это самая частая причина "ничего не меняется" на данный момент, не аппаратная |
| `TP_ReadRaw()` неверно реализует протокол XPT2046 (control byte / порядок бит / число dummy-тактов) | ⚠️ не исключена | Проект использует нестандартную самописную реализацию для диагностики (не готовую проверенную библиотеку тача) — ошибка в разборе 12-битного ответа даст именно "всегда 0", если, например, control byte собран неверно и чип его не распознаёт как валидную команду |
| `TP_CS` физически/логически не выбирает XPT2046 в момент чтения (регрессия ранее найденного бага "TP_CS с самого старта в LOW", сессия 2026-09-07) | ⚠️ не исключена, стоит перепроверить | Уже был один похожий баг с этим же пином в этом же проекте — стоит убедиться, что фикс всё ещё применяется именно в момент вызова `TP_ReadRaw()`, а не только один раз при `LCD_Init()` |
| Приём по MISO (PB14) на уровне SPI-периферии/пайки действительно не работает в принципе | ⚠️ не исключена, но пока не более вероятна, чем три пункта выше | Если верна — это была бы первая находка, что проблема не только в специфике самого ILI9486 (сдвиговые регистры), а в самом SPI2-приёме на STM32 стороне. Пока нет данных, чтобы выбрать именно эту версию среди четырёх |

**Как разделить эти версии, не меняя код (или с минимальной правкой только ради диагностики):**
1. Добавить в тот же цикл простой инкрементный счётчик (`volatile uint32_t heartbeat++`) и убедиться,
   что **он** меняется в Live Expressions во время нажатий — если не меняется даже счётчик, вопрос
   закрыт в пользу первой гипотезы (методика отладки), а не гипотез про SPI/тач.
2. Осциллографом посмотреть `TP_CS` (PC4) и `MISO` (PB14) именно в момент вызова `TP_ReadRaw()` —
   аналогично уже проделанной в сессии 2026-09-13 методике для остальных 6 линий, только теперь для
   ветки тача. Если на MISO вообще нет активности при выбранном TP_CS — это железо/пайка, а не логика.
3. Только после пп. 1–2 иметь смысл разбирать побайтово протокол `TP_ReadRaw()` против даташита
   XPT2046 — разбирать протокол раньше времени, не исключив тривиальные причины, будет повторением
   уже случившейся в проекте ошибки (гнаться за сложной гипотезой, не проверив простую).

## Итоговая таблица предложений (для решения пользователем — код не менять до отдельного запроса)

| # | Предложение | Статус проверки | Приоритет | Правка кода? |
|---|---|---|---|---|
| 1 | ~~Завершить тест из README (Live Expressions, `tp_x`/`tp_y` при касании)~~ — **выполнено 2026-09-20**: нажимал сильно, оба значения оставались нулевыми | ✅ тест проведён, ⚠️ результат неоднозначен (см. раздел выше — 4 равноправные гипотезы) | — | — |
| 1а | Добавить heartbeat-счётчик в цикл и проверить его в Live Expressions при том же тесте — разделяет "проблема методики отладки" от "проблема тача/SPI" | ⚠️ не проверено, простейший и самый быстрый шаг | Высокий (делать первым) | Минимальная, только диагностическая (одна переменная) |
| 1б | Осциллографом проверить `TP_CS`(PC4)/`MISO`(PB14) в момент `TP_ReadRaw()`, аналогично уже сделанной проверке 6 линий в сессии 2026-09-13 | ⚠️ не проверено | Высокий, после 1а | Нет |
| 1в | Перепроверить, что `TP_CS` реально выбран (LOW) именно во время `TP_ReadRaw()`, а не только один раз в `LCD_Init()` (прецедент похожего бага в сессии 2026-09-07) | ⚠️ не проверено | Средний, после 1а/1б | Нет, только чтение кода |
| 2 | Сверить не байты, а **структуру транзакций** заливки `RAMWR`: как именно референсы бьют поток пикселей на пакеты относительно CS (гипотеза из раздела выше) | ⚠️ не проверено | Высокий | Нет на этом шаге — только чтение/сравнение кода `LCD_Driver.cpp`/`LCD_Driver.c` |
| 3 | Отказаться от идеи снижать SPI-скорость записи как приоритетного шага | ❌ опровергнуто референсом (18 МГц Mode 0 работает) | — (действие: не тратить время) | — |
| 4 | Отказаться от идеи менять SPI Mode 0→3 как приоритетного шага | ❌ опровергнуто тем же референсом | — (действие: не тратить время) | — |
| 5 | Использовать `Drivers/lcd/ili9486/ili9486.c` из `RobertoBenjami` не как "готовую библиотеку", а как третий независимый источник для построчной сверки регистров инициализации (по аналогии с уже сделанной сверкой против Arduino и F103-демо) | ✅ репозиторий реальный, полезен как референс | Средний | Нет, только сверка |
| 6 | ~~Визуально сверить маркировку микросхем на обратной стороне шилда~~ — **выполнено 2026-09-20**: `74HC04D`, `74HC4040D`, 2×`74HC4094D` — совпадает с законом на 100% | ✅ ПОДТВЕРЖДЕНО напрямую (первичные данные, не литература) | — | — |
| 6а | (опционально, низкий приоритет) Осмотреть плату на предмет ДРУГИХ микросхем помимо уже найденных четырёх — в первую очередь: (а) маркировку самого XPT2046 рядом с тач-разъёмом и убедиться, что его MISO идёт напрямую на боковую гребёнку, в обход сдвиговых регистров; (б) наличие/отсутствие отдельного буфера-левел-шифтера (например `74HC125`/`74LVC245`) — это был открытый вопрос ("не исключено") в разделе про 3.3В/5В логику в README | ⚠️ не проверено | Низкий, не блокирует текущую диагностику тача | Нет |
| 7 | Держать в уме: `__NOP()`-гипотезу и гипотезу про FIFO/DataSize считать закрытыми (не подтверждены, есть прямое опровержение в рабочем стороннем коде) | ❌ опровергнуто | — (действие: не тратить время) | — |

## Статус документа

Этот файл — точка входа для оценки любой новой версии/гипотезы по этому проекту. При появлении
новых источников (форумы, схемы, чужой рабочий код) — сначала прогонять их через Правило №1 и
дописывать сюда, а не в `Readme.md` напрямую. `Readme.md` остаётся хроникой того, что уже сделано
и измерено руками (осциллограф, ST-Link, живые тесты) — это не отменяется и не противоречит Призме,
Призма лишь добавляет фильтр для новых теоретических предложений, в том числе от ИИ.
